# v2 ideas (park scope creep here, do NOT build during week 1)

- Protein / FASTA support (sequence identity instead of Tanimoto)
- Faster nearest-neighbor for big datasets (FAISS / approximate NN)
- Temporal-leakage check (needs a date column)
- More fingerprint types / configurable thresholds
- Hosted public demo with rate limiting
